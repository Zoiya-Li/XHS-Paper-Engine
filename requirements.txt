# XHS Paper Engine — Python Dependencies
# Python 3.9+
#
# Covers the full pipeline: search → dedup → select → download → PDF→text →
# figure extraction → write → vision optimization → publish.
#
# Figure/table extraction is done by pdffigures2, which runs as a self-contained
# Java JAR (no Python ML stack). It needs a Java runtime + the pdffigures2 JAR —
# see the README. The app refuses to start if those are missing.

# HTTP requests
requests>=2.31.0
aiohttp>=3.9.0

# Configuration management
pyyaml>=6.0.0
python-dotenv>=1.0.0

# PDF processing (text extraction, page rendering)
pymupdf>=1.23.0

# Image processing (vision tools, image handling)
Pillow>=10.0.0

# Browser automation
playwright>=1.40.0

# Markdown processing
markdown>=3.5.0

# Timezone handling
pytz>=2023.3
